version: 2
project_name: bouncerfox

builds:
  - id: bouncerfox
    main: ./cmd/bouncerfox
    binary: bouncerfox
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X main.version={{.Version}}
  - id: bf
    main: ./cmd/bouncerfox
    binary: bf
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: default
    ids: [bouncerfox, bf]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

homebrew_casks:
  - name: bouncerfox
    binaries:
      - bouncerfox
      - bf
    homepage: "https://github.com/bouncerfox/cli"
    description: "Scan AI agent config files for security and quality issues"
    repository:
      owner: bouncerfox
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
